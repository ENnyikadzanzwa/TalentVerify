// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#008080', // Replace with your primary color from the logo
    },
    secondary: {
      main: '#00bfff', // Replace with your secondary color from the logo
    },
  },
});

export default theme;
